name: New MCP server
version: 0.0.1
schema: v1
mcpServers:
  - name: bright-data-web
    command: npx
    args:
      - -y
      - "@brightdata/mcp"
    env:
      API_TOKEN: "bd_xxxxxxxxxxxxxxxx"  # ← Your actual token here